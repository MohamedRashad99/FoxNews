class Post {
  String title ;
  String description;
  String publishedAt;
  String urlToImage;
  String author;

  Post( { this.title, this.description, this.publishedAt, this.urlToImage, this.author} );
}